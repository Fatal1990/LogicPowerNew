// Не переводится
export enum DeliveryMethod {
	warehouse = 'warehouse',
	courier = 'courier',
}

// Это переводится
export enum ViewDeliveryMethod {
	warehouse = 'warehouseMethod',
	courier = 'courierMethod',
	pickup = 'pickupMethod',
	deliveryWithinOdessa = 'deliveryWithinOdessaMethod',
}
